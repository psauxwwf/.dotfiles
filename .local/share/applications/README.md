```bash
sudo ln -sf "$(which hx)" /usr/local/bin/hx
sudo ln -sf ~/.local/kitty.app/bin/kitty /usr/local/bin/kitty
xdg-mime default helix.desktop text/markdown
xdg-mime default helix.desktop text/x-markdown
```
